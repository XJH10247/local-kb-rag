# 安全策略

## 报告漏洞

**请勿用公开 Issue 报告安全问题。**

本项目已启用 GitHub 的私下漏洞报告功能：打开仓库的 **Security** 标签页，
点击 **Report a vulnerability**，即可与维护者私下沟通。

报告中请尽量包含：

- 受影响的版本（`skills/local-kb-rag/meta.json` 的 `version`）
- 问题类型与影响（例如：本机权限提升、named pipe 被非授权进程接管、
  恶意文档触发解析器越界、依赖供应链问题）
- 可复现的最小步骤或 PoC
- 你所评估的严重程度与理由

维护者会尽快确认收到并同步处理进展；在修复发布前，请不要公开披露细节。

## 威胁模型与信任边界

理解本项目的设计边界有助于判断某个问题是否属于安全缺陷：

| 组件 | 信任假设 |
|---|---|
| 语料文件（PDF / Markdown / TXT） | **不可信输入**。解析与切分属于攻击面，尤其 PDF 解析（PyMuPDF） |
| named pipe `\\.\pipe\local-kb-rag` | **仅本机可访问**，用固定 authkey 做防误连而非防对抗 |
| 本地 HTTP / 网络 | 不存在。全链路不走网络，除首次通过 ModelScope 下载模型权重 |
| 运行时数据目录 | `%USERPROFILE%\.openvino\` 下，依赖操作系统账户隔离 |
| 模型权重 | 从 ModelScope 下载，需自行确认上游来源与完整性 |

## 设计上的安全取舍

- **authkey 不是访问控制**：`common.AUTH_KEY` 是源码内常量，只用于防止无关进程
  误连，不作为对抗同机恶意进程的认证手段。同一台机器上的同权限进程可以连接该 pipe。
- **本机单用户模型**：不以多租户或跨账户隔离为目标。请勿把运行时数据目录暴露给
  其他权限主体。
- **第三方模型与依赖**：BGE-M3、BGE-reranker-v2-m3、Qwen2.5 及各依赖库
  各自遵循上游许可证与安全策略；下载与使用前请自行确认。

## 支持的版本

安全修复只面向最新发布版本（见 [CHANGELOG.md](CHANGELOG.md)）。
建议始终运行主分支或最新版本。

## 隐私

语料、向量库与模型权重全部驻留本机；除答案文本回传 Agent 宿主外，
不会有任何内容离开本机。首次模型下载完成后可完全离线运行。
